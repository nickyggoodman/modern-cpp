#include <iostream>

int main() {
  int* ptr = NULL;
  int* ptr2 = nullptr;
  
  int number = NULL; // NULL is 0
  //int n2 = nullptr; // compile error. nullptr not convertible to an int

  //int* ptr3 = true ? 0 : nullptr; // compile error. incopatible types.

}
